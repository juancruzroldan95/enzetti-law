---

---

<div
  class="instagram-stats bg-gradient-to-r from-purple-600 to-pink-600 text-white p-6 rounded-xl shadow-lg flex items-center justify-between"
>
  <div class="flex items-center gap-4">
    <svg
      xmlns="http://www.w3.org/2000/svg"
      width="32"
      height="32"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"
      class="feather feather-instagram"
      ><rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect><path
        d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path><line
        x1="17.5"
        y1="6.5"
        x2="17.51"
        y2="6.5"></line></svg
    >
    <div>
      <p class="text-sm font-medium opacity-90">Síguenos en Instagram</p>
      <p class="text-2xl font-bold" id="insta-count">Cargando...</p>
    </div>
  </div>
  <a
    href="https://instagram.com"
    target="_blank"
    rel="noopener noreferrer"
    class="bg-white/20 hover:bg-white/30 px-4 py-2 rounded-lg text-sm font-medium transition-colors"
  >
    Ver Perfil
  </a>
</div>

<script>
  // Placeholder for Instagram API integration
  // TODO: Replace with real API call
  async function fetchInstagramStats() {
    try {
      const response = await fetch("/api/instagram");
      const data = await response.json();
      const countElement = document.getElementById("insta-count");
      if (countElement) {
        countElement.innerText = data.followers || "10.5k";
      }
    } catch (error) {
      console.error("Error fetching Instagram stats:", error);
      const countElement = document.getElementById("insta-count");
      if (countElement) {
        countElement.innerText = "10.5k"; // Fallback
      }
    }
  }

  fetchInstagramStats();
</script>
